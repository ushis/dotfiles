# ~/.zsh/functions/flag

# Upload files to flag.io
local f url='http://flag.io'

if [ $# -eq 0 ]; then
  curl -T - "${url}"
  return $?
fi

for f in $*; do
  if [ ! -r "${f}" ]; then
    print "Can't read:" "${f}" 1>&2
  else
    curl -T "${f}" "${url}"
  fi
done
