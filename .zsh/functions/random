# ~/.zsh/functions/random

# Fallback to 64, if no length is specified.
local n=64; [ $# -gt 0 ] && n="${1}"

# We want all printable characters, but not: whitespace \ [ ] ' "
echo "$(tr -d "\\\[\]\'\"" </dev/urandom | tr -cd '[:graph:]' | head -c "${n}")"
